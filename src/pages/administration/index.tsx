import PlaceholderPage from '../../components/common/PlaceholderPage';

export default function AdministrationPage() {
  return (
    <PlaceholderPage
      eyebrow="Administration"
      title="Administration workspace is ready"
      description="This route is intentionally minimal so the administration experience can be added later without inheriting the previous app structure."
    />
  );
}
