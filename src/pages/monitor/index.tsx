import PlaceholderPage from '../../components/common/PlaceholderPage';

export default function MonitorPage() {
  return (
    <PlaceholderPage
      eyebrow="Monitor"
      title="Monitor workspace is ready"
      description="This folder is intentionally clean and ready for the next screen. The navbar route is wired up, but the page content is still empty by design."
    />
  );
}
