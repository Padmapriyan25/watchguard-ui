import PlaceholderPage from '../../components/common/PlaceholderPage';

export default function ConfigurePage() {
  return (
    <PlaceholderPage
      eyebrow="Configure"
      title="Configure workspace is ready"
      description="This route has a fresh starter page so we can build configuration flows next without carrying over any of the previous UI."
    />
  );
}
