import PlaceholderPage from '../../components/common/PlaceholderPage';

export default function InventoryPage() {
  return (
    <PlaceholderPage
      eyebrow="Inventory"
      title="Inventory workspace is ready"
      description="The inventory page folder is in place and connected to the navbar. We can now add inventory tables, filters, or drilldowns on top of this fresh setup."
    />
  );
}
